type ShopOfNumbers = {
    name: string,
    items: Array<number>;
}

type ShopOfSrings = {
    name: string,
    items: Array<string>;
}

type showOfBooleans = {
    name: string,
    items: Array<boolean>;
}

